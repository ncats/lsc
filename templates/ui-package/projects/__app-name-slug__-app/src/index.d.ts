// so the typescript compiler doesn't complain about the global config object
// app configuration injected from shell-ui
declare let APP_CONF: any;
// project build version injected from shell-ui
declare let APP_BUILD_VERSION: string;
// project app type injected from shell-ui
declare let APP_TYPE: any;
